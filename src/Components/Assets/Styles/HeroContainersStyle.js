import { makeStyles } from '@material-ui/core/styles';

const HeroContainersStyle = makeStyles(theme => ({
  homeRoot: {
    margin: 0,
    width: '100%',
    position: 'relative',
  },
  brnadTitle: {
    margin: '100px',
  },
}));

export default HeroContainersStyle;
